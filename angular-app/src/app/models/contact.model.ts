import { Component } from '@angular/core';

export interface Contact {
    id: number;
    email: string;
    first_name: string;
    last_name: string;
    n_document: string;
    phone_number: string;
}
